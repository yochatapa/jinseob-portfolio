<script setup lang="ts">
</script>

<template>
    <div class="profile-card-detail">
        CardDetail
    </div>
</template>

<style scoped>
/* .profile-card-detail {
    --card-width: 16;
    --card-height: 9;

    height: calc(100% - var(--Spacer-30));
    aspect-ratio: calc(var(--card-width) / var(--card-height));
    border: 1px solid red;
} */

/* 모바일 (최대 768px) */
/* @media (max-width: 1024px) {
    .profile-card-detail {
        display: none;
    }
} */

/* 태블릿 (768px ~ 1024px) */
/* @media (max-height: 600px){
    .profile-card-detail {
        display: none;
    }
} */

/* 가로 < 1.8 * 세로 */
/* @media (max-width: calc(1.8 * 100vh)) {
    .profile-card-detail {
        display: none;
    }
} */

/* 세로 화면 (세로 방향) */
/* @media (orientation: portrait) {
    .profile-card-detail {
        display: none;
    }
} */
</style>
